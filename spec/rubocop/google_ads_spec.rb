RSpec.describe Rubocop::GoogleAds do
  it "has a version number" do
    expect(Rubocop::GoogleAds::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
